# Assets

Static images used by the site.

- `hero.jpg` — homepage hero background, referenced by `index.html`.
  Recommended: landscape, ~1920px wide, optimized JPG (under ~500 KB).
  To replace the homepage image, overwrite this file (keep the same name).
